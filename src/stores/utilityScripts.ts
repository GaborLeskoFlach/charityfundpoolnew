export class UtilityScripts{
    //await this.props.controller.addTypesOfWork({ label: 'Light duty work', value : '0', active : true  })
    //await this.props.controller.addTypesOfWork({ label: 'Medium duty work', value : '1', active : true  })
    //await this.props.controller.addTypesOfWork({ label: 'Heavy duty work', value : '2', active : true  })

    /*
    @action("add WhatWeNeed data to DB")
    addWhatWeNeed = (value : IWhatWeNeed) => {
        return new Promise<boolean>((resolve) => {
            _firebaseApp.database().ref('utils/whatWeNeed').push(value).then((response) => {
                resolve(response)
            })
        })
    }

    @action("add typesOfWork data to DB")
    addWhatINeedHelpWith = (value : IWhatINeedHelpWith) => {
        return new Promise<boolean>((resolve) => {
            _firebaseApp.database().ref('utils/whatINeedHelpWith').push(value).then((response) => {
                resolve(response)
            })
        })
    }   */

    /*
    await this.props.controller.addWhatINeedHelpWith({ label: 'Cleaning', value : 'Cleaning', active : true  })
    await this.props.controller.addWhatINeedHelpWith({ label: 'Gardening', value : 'Gardening', active : true  })
    await this.props.controller.addWhatINeedHelpWith({ label: 'Shopping', value : 'Shopping', active : true  })
    await this.props.controller.addWhatINeedHelpWith({ label: 'Going to places', value : 'Going to places', active : true  })
    */
    
    /*
    @action("add typesOfWork to DB")
    async addTypesOfWork(typeOfWork : ITypeOfWork){
        return new Promise<Array<ITypeOfWork>>((resolve) => {
            _firebaseApp.database().ref('utils/typesOfWork').push(typeOfWork).then((response) => {
                resolve(response)
            }) 
        })
    } */


}


